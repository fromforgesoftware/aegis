replicaCount: 2

database:
  host: '' # set by the deploy pipeline / env overlay
  name: aegis
  schema: aegis
  ssl: require
  existingSecret: aegis-db

autoscaling:
  enabled: false

podDisruptionBudget:
  enabled: true
  minAvailable: 1
